export const perfectSore = 100;

export const sum = (num1, num2) => {
  return num1 + num2;
}

export const avege = (num1, num2) => {  
  return (num1 + num2) / 2;
}   
const subtract = (num1, num2) => {
  return num1 - num2;   
}

export default {
    subtract
};